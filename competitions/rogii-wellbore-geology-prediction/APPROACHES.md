# Approaches

Track modeling approaches, experiments, submissions, and outcomes here. Prefer short entries with enough detail that a future chat can understand what was tried and whether it is worth revisiting.

## Current Best

| Date | Approach | Local CV | Public LB | Private LB | Notebook/commit | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-10 | Triple-signal beam search + dual PF + LightGBM (likely) | TBD | 11.107 | TBD | `notebooks/triple-signal-beam-search-dual-pf-lightgbm.ipynb` | Best submitted score so far (submission `52524349`, 2026-05-10 19:29:51). Notebook attribution inferred from kernel run time (19:27:33) being closest to the submission timestamp — not yet confirmed via kernel version metadata. |

## Tried

| Date | Approach | Changes | Local CV | Public LB | Outcome | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-06 | NN starter (`nn-starter-cv-15-5.ipynb`) | Simple PyTorch baseline, CV ~15.5 | ~15.5 | 15.744 | Submission `52390182`; matches notebook's own CV-15.5 name | Superseded by later submissions |
| 2026-05-10 | Unidentified second submission same session | TBD | 11404.716 | Failed/anomalous | Submission `52524355`, 2026-05-10 19:30:03 — 12 seconds after the 11.107 submission, score is ~1000x worse, almost certainly a broken run (wrong units/column, or an unintended submission file) | Identify which notebook produced this and why it broke before reusing it |
| 2026-07-23 | Rebuild of shared public pipeline, `notebooks/rogii-lb7295-public-rebuild.ipynb` run as-is (GPU off, CPU fallback confirmed working), contact-gated override profile "aggressive" | Pushed as Kaggle kernel `dalloliogm/rogii-lb7295-rebuild-catchup` v1 with datasets `koolbox-offline`, `rogii-claude-models-pub`, `wellbore-geology-prediction-artifacts`. Log showed contact override fired for all 3 test wells with near-perfect prefix reconstruction (RMSE 0.008-0.01 ft, 100% of rows overridden per well). Submitted `submission.csv` manually via Kaggle website (this is a **notebook-only/code competition** — raw `kaggle competitions submit -f` fails with a 400 "Submission not allowed" error; must submit through a kernel's Output tab or `-k/-v` CLI flags) | TBD (not run locally) | Submission `54927668`, pending as of 2026-07-23 12:19 | Pending — this is our first real test of whether the shared pipeline reproduces its claimed ~7.3 score for us | Check score once scored; if it lands near 6-9 as expected, this confirms Phase 0 and we move to Phase 1/2 |
| 2026-07-23 | Model-package-only ablation: `submission_model_package_only.csv` from the `working-note` kernel's own output (pure pretrained model-package/TabICL prediction, no PF, no override) submitted manually via that kernel's Output tab | Chosen because it differs from the main override-based submission by mean 15.1 ft / max 29.3 ft — a genuinely distinct model family, not a near-duplicate | `dalloliogm/rogii-working-note-geosteering-catchup` v1, file `submission_model_package_only.csv` | Submission `54929149`, 2026-07-23 13:42, pending | Pending | |
| 2026-07-23 | More advanced sibling pipeline, `notebooks/working-note-target-free-tvt-geosteering.ipynb` (adds visible-prefix calibration, bimodal hedge, model-package/TabICL blending, 7 dataset sources) | Pushed as `dalloliogm/rogii-working-note-geosteering-catchup` v1 | N/A | **Not submitted — its `submission.csv` is byte-identical (sha256 `fdf4a817...`) to the simpler rebuild above.** The same-well contact override fires perfectly for all 3 test wells in both notebooks and completely overwrites whatever the PF/learned-branch/model-package/TabICL layers computed upstream; the model-package correction even self-gated off (log: `model package p95 diff 26.701 > 25.000`). | No new information from this notebook for these 3 wells | Confirmed (see row below) that no existing profile disables the override — led directly to the no-override ablation fork |
| 2026-07-23 | No-override ablation: forked the working-note pipeline above with one line changed (`RUN_GUARDED_OVERLAP_OVERRIDE = False`, forced regardless of profile) to see what it scores *without* the same-well contact shortcut | Confirmed via grep that every `PROFILE_PRESETS` entry has `run_guarded_overlap_override=True` — there's no existing config that skips it, and both the direct override (cell 48) and its "gold" reapplication (cell 50) are gated by the same flag via the `ROGII_GOLD_CONTACT_OVERRIDE` env var, so this one-line patch cleanly disables both applications | New file `notebooks/working-note-no-override-ablation.ipynb`, pushed as `dalloliogm/rogii-no-override-ablation` v1 | Submission `54935192`, 2026-07-23 19:39, pending. `submission.csv` (sha256 `009d757f...`) differs from the override-based one by mean 1.4 ft / max 6.0 ft across ~99% of rows — meaningfully different, same general ballpark | Pending | Note found while investigating: the "visible-prefix calibration" stage runs *after* the SP45/learned blend and re-selects a candidate by its own holdout RMSE — so patching `SP45_BLEND_WEIGHT` alone would NOT reliably force a specific blend to survive to the final file. Used a more robust pattern instead (see next two rows): let the full pipeline run, then append one final cell that force-overwrites `submission.csv` with the exact file wanted. |
| 2026-07-23 | Pure-learned-branch ablation: `working-note-no-override-ablation.ipynb` + one appended final cell (`shutil.copy('learned_trajectory_submission.csv', 'submission.csv')`) | Isolates the LightGBM/CatBoost booster ensemble alone (no PF/ridge anchor, no override, no model-package) as the actual leaderboard submission | New file `notebooks/working-note-pure-learned-ablation.ipynb`, pushed as `dalloliogm/rogii-pure-learned-ablation` v1 | Submission `54935198`, 2026-07-23 19:39, pending. Verified `submission.csv` byte-identical to `learned_trajectory_submission.csv` (sha256 `70e34f3c...`), 14151 rows, id order matches sample, mean 11905.28 | Pending | |
| 2026-07-23 | Pure-anchor ablation: same base + final cell copying `sp45_projection_submission.csv` instead | Isolates the PF/ridge/selector anchor alone (the other end of the SP45↔learned blend spectrum) | New file `notebooks/working-note-pure-anchor-ablation.ipynb`, pushed as `dalloliogm/rogii-pure-anchor-ablation` v1 | Submission `54935199`, 2026-07-23 19:39, pending. Verified `submission.csv` byte-identical to `sp45_projection_submission.csv` (sha256 `08d2eae4...`), 14151 rows, id order matches sample, mean 11904.05 | Pending | |

## Backlog

| Idea | Rationale | Expected impact | Cost | Priority |
| --- | --- | --- | --- | --- |
| Adopt/adapt the shared "target-free geosteering" pipeline (PF/beam/DTW + GBM residual + contact override; see `LEARNINGS.md` and `notebooks/working-note-target-free-tvt-geosteering.ipynb`) | Public meta moved from ~11-15 (May) to 6-9 (July) via this technique; hundreds of forks confirm it reproduces reliably; GPU not required | Likely jump from rank 3504 to top-25%/bronze in one submission | Medium (adaptation, not invention — `koolbox`/artifacts datasets do the heavy lifting) | High |
| Ensemble our own dual-PF/LightGBM notebook + the DWT-based lineage on top of the shared pipeline | Most forkers only tweak one shared notebook's config; independent signals are the realistic path from bronze to silver/gold | Differentiation beyond the fork cluster | Medium-High | High |
| Validate via visible-prefix holdout instead of LB probing | Competition-specific per-well self-calibration signal already built into the public notebooks; submissions capped at 5/day | Avoid wasting submission budget | Low | High |
| Compare starter NN, TCN, and LightGBM under one grouped CV split | Current older notebooks likely use different assumptions and validation setups; may be superseded by the geosteering approach | Establish reliable baseline ranking | Medium | Low (deprioritized vs. adopting the current meta) |
| Audit online/test-known-zone training logic | The strongest-looking notebook uses test known zones for calibration-style samples | Avoid leakage or rule violations | Medium | Medium |
| Build an end-to-end Kaggle submission notebook | Existing notebooks may be exploratory or runtime-heavy | Make submissions reproducible | Medium | Medium |

## Abandoned

| Approach | Why dropped | Evidence | Revisit if |
| --- | --- | --- | --- |
| TBD | TBD | TBD | TBD |
