# Approaches

Track modeling approaches, experiments, submissions, and outcomes here. Prefer short entries with enough detail that a future chat can understand what was tried and whether it is worth revisiting.

## Current Best

| Date | Approach | Local CV | Public LB | Private LB | Notebook/commit | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-14 | Fresh-validated Udit22 targeted graft on scored `7329.97` exact base | Ranked the 22 task-targeted donor directories. Eleven variants were locally cost-positive; fresh generation rejected `task205` at 126/128 and `task233` at 124/128. The nine survivors passed both 128- and 896-request blocks: 8,828/8,828 generated in-bounds examples correct, with 388 oversized `task184` examples skipped. Archive passed canonical layout, CRC, 400/400 standard ONNX checks, and full checks on all changed tasks. | 7330.16, submission ref `54673781` | Unknown | `scripts/rank_targeted_variant_directories.py`; `submissions/udit22-nine-stress/submission.zip` | New current best, `+0.19` over `54673593`, exactly matching the displayed precision of the `7330.160293` projection. Changed `task008`, `task035`, `task044`, `task184`, `task222`, `task270`, `task275`, `task281`, and `task323`; SHA-256 `fc99e9055f5a226e329d46941783356e9218b7a5b845e87d6af77c7bb0ae9bcc`. |
| 2026-07-14 | Exact scorer rewrites on scored `7329.90` Ryosuke graft | Re-ran the exact rewrite toolchain over all 400 tasks. Twelve accepted rewrites reduced scorer cost by 114 and projected `+0.067639`; all 12 passed 1,024/1,024 fresh ARC-GEN examples, standard ONNX checks, and full changed-task checks. | 7329.97, submission ref `54673593` | Unknown | `scripts/exact_rewrite_toolchain.py`; `submissions/exact-rewrite-7329-v1/submission.zip` | New current best, `+0.07` over `54673405`, matching the displayed precision of the projection. Changed `task008`, `task090`, `task153`, `task165`, `task195`, `task250`, `task268`, `task273`, `task346`, `task368`, `task383`, and `task388`; SHA-256 `10cf68abee880a1156fa269fc1c893b97b89bf3e0d1737eaabff88de6a1d845d`. |
| 2026-07-14 | Fresh-validated Ryosuke `7281.18` task graft on scored `7320.02` base | Ranked all 400 donor tasks and found 74 locally cost-positive replacements projecting `+9.884902`. All 74 passed independent 128- and 896-request ARC-GEN blocks: 75,763/75,763 generated in-bounds examples correct, with 13 oversized `task055` examples skipped. Archive passed canonical layout, CRC, 400/400 standard ONNX checks, and full checks on all changed tasks. | 7329.90, submission ref `54673405` | Unknown | `scripts/rank_bundle_variants.py`; `scripts/build_ranked_graft_bundle.py`; `submissions/ryosuke-7281-positive-screen/submission.zip` | New current best, `+9.88` over `54661951`, exactly matching the displayed precision of the `7329.904902` projection. SHA-256 `7fc66b3cb949ca501077fa4971b60a21ec299291b3e71db5969427f06a3d148a`. |
| 2026-07-13 | Cross-source cost graft plus `task243` carry on scored `7319.89` base | Ranked 699 unique variants from 21 public archives. Five replacements passed 1,024/1,024 fresh ARC-GEN cases each and projected `+0.014643`; `task243` projected `+0.107337` and improved the matched 1,024-case control from 1,021 to 1,022 correct with failures strictly reduced from `[11, 910, 964]` to `[11, 964]`. Archive passed canonical layout, CRC, 400/400 standard ONNX checks, and full checks on all six changed tasks. | 7320.02, submission ref `54661951` | Unknown | `scripts/rank_cross_source_variants.py`; `scripts/build_ranked_graft_bundle.py`; `submissions/cross-source-five-task243-final/submission.zip` | New current best, `+0.13` over v9 graft v2, closely matching the combined `+0.121980` projection. SHA-256 `f8f03cb1b0d13ac787d0875d638ee20b00957ffd86b560104de2ad85f37d9537`. |
| 2026-07-13 | Second-wave fresh-ARC-GEN v9 graft on scored `7299.68` base | Fifty-two replacements passed 1,024/1,024 newly generated cases each. Forty-six measurable changes projected `+20.702662`; six additional variants passed functionally but had unresolved local deltas. Archive passed canonical layout, CRC, and standard ONNX checker. | 7319.89, submission ref `54661618` | Unknown | `scripts/rank_bundle_variants.py`; `scripts/validate_arc_gen_fresh.py`; `submissions/v9-fresh-gen-graft-v2/submission.zip` | New current best, `+20.21` over the first-wave v9 graft and `+50.09` over exact-rewrite v3. Live gain was within `0.49` of the measurable projection. SHA-256 `62ec43744d8ecea00a2db70c1cf9efaba1198c4b655df89e7abb28bcdc8032ce`. |
| 2026-07-13 | Fresh-ARC-GEN-validated public v9 graft on scored v3 | Seventeen replacements passed 1,024/1,024 newly generated ARC-GEN examples per task; 15 scoreable changes projected `+25.813616`, while negative-padding `task045`/`task240` were held to functional validation. Archive passed canonical layout, CRC, and standard ONNX checker. | 7299.68, submission ref `54660934` | Unknown | `scripts/validate_arc_gen_fresh.py`; `submissions/v9-fresh-gen-graft-v1/submission.zip` | New current best, `+29.88` over v3. Fresh task-domain generation successfully filtered three rare-failure variants and recovered about `+4.07` extra from the two locally unscorable accepted tasks. SHA-256 `fba281ad3d9a5719834467a4c6b23a99575127b8d487ed14be745107ca0a8950`. |
| 2026-07-13 | Exact rewrite pass v3 on scored `7269.74` base | Four changed tasks passed all available examples with raw-output equality; randomized exact checks: `task206` 5/16 with 11 symmetric task-invalid failures, `task208` 14/16 with 2 symmetric failures, `task222` 16/16, and `task243` 128/128. Archive validation passed 400/400 ONNX checker, canonical root layout, and zip CRC. | 7269.80, submission ref `54654005` | Unknown | `scripts/exact_rewrite_toolchain.py`; `submissions/exact-rewrite-pass-v3-validated/submission.zip` | New current best. Stacks exact scorer improvements on `task206`, `task208`, `task222`, and `task243`; cumulative cost delta `-160`, local point delta `+0.054862`; zip SHA-256 `5a6cffc666895ac0cd79922fcd31db0e11cf1d0fa2fed3d115b27aa6f062c866`. |
| 2026-07-13 | Exact stale-value-info rewrite pass v1 on current best | Full 400-task scan; candidate outputs matched base outputs on local examples for accepted tasks; archive validation passed with 400 root ONNX files and zip CRC clean. | 7269.74, submission ref `54652438` | Unknown | `scripts/exact_rewrite_toolchain.py` over `lucifer-plus-kaiwalya-six-no205` | New current best; accepted stale value-info cleanup on `task085`, `task105`, `task237`, `task355`, `task370`, and `task396`; local estimated point delta `+0.134907`; output zip SHA-256 `a497085e3d462908302ca44e72220b157398d2c49f3c93474380beb7af61a2de`. |
| 2026-07-13 | Lucifer plus Kaiwalya six-task graft, excluding `task205` | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. | 7269.61, submission ref `54646591` | Unknown | Local graft from `lucifer19/neurogolf-agi-circuit-forge` plus `kaiwalyaatulraut/neurogolf-championship-solution` | New current best seen in this workspace; output zip SHA-256 `e547ca18dfecca166236a0ef86f9ec42d530a0288877a58640c0db538ae63894`. |
| 2026-07-13 | Public Lucifer AGI Circuit Forge artifact | Notebook log reported `400/400` ONNX checker pass, static IO pass, changed-model ORT load pass, and 400 bit-exact ORT trials for accepted rewrites on `task205` and `task368` | 7269.60, submission ref `54645666` | Unknown | Public notebook `lucifer19/neurogolf-agi-circuit-forge` | New current best seen in this workspace; output zip SHA-256 `644938043f8b9d729235e66dfeaef7ed586a69ca9cc4c198cb5164f81b3b3ad0`. |
| 2026-07-12 | Public Poby 7268 artifact | Kaggle notebook log reported archive integrity OK; local archive inspection found 400 root ONNX files | 7268.00, submission ref `54619344` | Unknown | Public notebook `poby7722/7268-neurogolf-best-score` | Former account best; now superseded by public Lucifer artifact `54645666`. |
| 2026-07-09 | Account baseline artifact | Not reproduced locally from source; output zip has 400 task files | 7267.32, submission ref `54487100` | Unknown | Kaggle kernel `dalloliogm/2026-neurogolf-baseline` | Former account best; now superseded by public Lucifer artifact `54645666`. |
| 2026-07-10 | Color-map 1x1 convolution baseline | 4 task networks pass all provided examples | 81.57, submission ref `54528731` | Unknown | `scripts/build_color_map_submission.py` | Generates `submission.zip` for `task016`, `task276`, `task309`, and `task337`; useful mechanics anchor, not final-worthy. |

## Tried

| Date | Approach | Changes | Local CV | Public LB | Outcome | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-10 | Pure color-map task scan | Scanned all 400 JSON files for same-shape tasks where output is a consistent per-color mapping across train/test/arc-gen. | Found and validated 4 tasks. | 81.57, submission ref `54528731` | Completed and scored, but far below existing account score `7267.32`. | Expand to affine/geometric/local-neighborhood transforms; do not rely on four-task package. |
| 2026-07-10 | `own7267-plus-ryosuke-7` graft | Replaced seven smaller task files from public close-scoring `ryosuke-7266-48`: `task005`, `task008`, `task107`, `task233`, `task310`, `task366`, `task370`. Candidate zip was 25,274 uncompressed bytes smaller than the account baseline. | The seven replacements passed all official-valid provided examples under NeuroGolf 30x30 one-hot semantics. | 7267.19, submission ref `54529022` | Worse than the account baseline by 0.13 public LB points, so at least one replacement loses hidden correctness or score. | Do not use this full graft as final. Test future grafts in smaller isolated groups. |
| 2026-07-10 | `ryosuke-7266-48` lower-risk split tests | Tested smaller subsets from the failed seven-task graft: five-task `task005/task008/task107/task310/task370`, A3 `task005/task008/task107`, and B2 `task310/task370`. | All selected replacements passed official-valid provided examples before submission. | Five-task `54529420`: 7267.27; A3 `54529552`: 7267.29; B2 `54529559`: 7267.29 | No tested subset beats the account baseline `54487100` at 7267.32. | Stop broad Ryosuke grafting; look for genuinely new task improvements or another public source with stronger evidence. |
| 2026-07-12 | Frank `task074` single-task probe | Replaced only `task074` from public `franksunp/7245-63-lb-neurogolf-variant-b-mark-a`; candidate shrank `task074` from 8001 to 708 bytes. | Replacement passed all 267 provided examples. | 7267.24, submission ref `54619246` | Failed despite local validation and large byte reduction; likely hidden correctness loss or worse hidden scoring. | Do not treat byte reduction as safety evidence; prefer higher-scoring public artifacts or source-level improvements. |
| 2026-07-12 | Public Poby 7268 artifact | Submitted full output from public `poby7722/7268-neurogolf-best-score`; differs from the 7267.32 baseline on 25 tasks, with net uncompressed delta `-53687` bytes. | Archive had 400 root ONNX files and notebook log reported integrity OK. | 7268.00, submission ref `54619344` | Improves the account best by `+0.68` over `54487100`. | Use as the current best candidate unless a later submission beats it. |
| 2026-07-13 | Poby six-task larger-file rollback | Started from Poby `54619344` and rolled back the six tasks where Poby was larger than the old baseline: `task008`, `task035`, `task044`, `task074`, `task085`, `task363`. Candidate was 9,820 uncompressed bytes smaller than Poby. | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. | 7267.90, submission ref `54638892` | Worse than Poby by 0.10, so grouped rollback removes hidden-value changes. | Do not use this candidate. If spending more slots, isolate individual rollbacks or use fresher public sources. |
| 2026-07-13 | Public Lucifer AGI Circuit Forge artifact | Submitted full output from `lucifer19/neurogolf-agi-circuit-forge`; archive differs from Poby on 61 tasks and notebook audit claims exact accepted rewrites on `task205` and `task368` over a `7269.56` anchor. | Notebook log reports 400/400 checker pass and 400 bit-exact ORT trials. | 7269.60, submission ref `54645666` | Improves over Poby by `+1.60` and becomes the new current best. | Use this artifact as the baseline for further source mining or task-level grafts. |
| 2026-07-13 | Lucifer plus Kaiwalya six-task graft, excluding `task205` | Started from Lucifer Circuit Forge and replaced only Kaiwalya `task018`, `task090`, `task105`, `task133`, `task174`, and `task355`; kept Lucifer `task205` because its audit claims a stronger scorer-cost rewrite. Candidate changed six files vs Lucifer and has SHA-256 `e547ca18dfecca166236a0ef86f9ec42d530a0288877a58640c0db538ae63894`. | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. | 7269.61, submission ref `54646591` | Improves over Lucifer Circuit Forge by `+0.01` and becomes the new current best. | Use as the baseline for further grafting; next useful step is to isolate/stack other public exact rewrites rather than reverting `task205`. |
| 2026-07-13 | Anas explicit rewrite graft on current best | Started from current best `54646591` and replaced Anas `task054`, `task101`, and `task396`. Candidate SHA-256 `d59f1f6a6d30652081c67ba00da2239bb879f62218c32078cb74550c972c4a79`. | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. Anas public manifest claimed all changed tasks passed its validation, but against a much lower `7267.99` source. | 7269.59, submission ref `54647024` | Worse than current best by `0.02`. | Do not broaden Anas on this base. |
| 2026-07-13 | Isolated Anas `task054` graft | Started from current best and replaced only Anas `task054`; candidate SHA-256 `4c4c06673a5927b4373d6e927ad2ef3f2ce8da4d1cd2674fe69eada8cad862fc`. | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. | 7269.59, submission ref `54647140` | Same displayed score as the three-task Anas bundle, confirming `task054` is harmful on the current base. | Do not submit Anas `task101`/`task396` next; their combined effect with harmful `task054` was not enough to move score upward. |
| 2026-07-13 | Jonathan explicit `task197`/`task264` graft | Started from current best and replaced only the named grafted models from `jonathanncoletti/neurogolf-7242-task-graft-with-explainations`; `task236` was already byte-identical to current. Candidate SHA-256 `33e652194ad79e356a1595157f3b6c1b3932b4e50c07c4e40033c0ad3f9a9598`. | Archive validation passed: 400 canonical root ONNX files and zip CRC clean. | 7269.54, submission ref `54647351` | Worse than current best by `0.07`; lower-score public grafts do not transfer safely to the Lucifer/Kaiwalya base. | Do not use Jonathan lower-base grafts unless a future source gives exact proof against the current base. |
| 2026-07-13 | Exact stale-value-info rewrite pass v1 | Ran `scripts/exact_rewrite_toolchain.py` over the current best bundle. Accepted only tasks where candidate outputs matched the base on local examples and official-style cost decreased. | Accepted `task085`, `task105`, `task237`, `task355`, `task370`, and `task396`; local cost delta `-192`; local point delta `+0.134907`; candidate SHA-256 `a497085e3d462908302ca44e72220b157398d2c49f3c93474380beb7af61a2de`. | 7269.74, submission ref `54652438` | Improves current best by `+0.13`; local point estimate matched displayed public LB movement closely. | Extend this toolchain with additional exact graph rewrites rather than returning to blind public grafts. |
| 2026-07-13 | Expanded exact rewrite passes v2/v3 | Added scorer-aware Cast pushdown, Concat/Cast fusion, duplicate initializer/node removal, reshape-chain cleanup, constant `Where`/identity cleanup, commutative CSE, and bitwise absorption. Scanned all 400 tasks from the scored v1 base. | Accepted `task206` (`-8` cost), `task208` (`-16`), `task222` (`-120`), and `task243` (`-16`); cumulative local point delta `+0.054862`. Final candidate passed 400/400 checker and differs from v1 on exactly those four tasks. | 7269.80, submission ref `54654005` | Improved the scored base by `+0.06`, closely matching the local estimate; SHA-256 `5a6cffc666895ac0cd79922fcd31db0e11cf1d0fa2fed3d115b27aa6f062c866`. | Use v3 as the baseline for further exact scorer-aware work; do not replace any accepted rewrite with a byte-smaller public model. |
| 2026-07-13 | Leaderboard variant deconvolution | Fit an additive ridge model to 13 scored archive comparisons across 107 task-variant features, plus controlled pair constraints, then ranked 625 public task variants. | In-sample RMSE `0.00139`, but leave-one-out RMSE `0.05711`; only two exact single-task score constraints were available. | Not submitted | Useful only as an audit queue. It correctly keeps known harmful `task074` and Anas `task054` evidence visible, but cannot reliably attribute small positive task effects. | Require direct scorer improvement and functional validation before grafting any ranked variant. |
| 2026-07-13 | Targeted public audit of `task243` and other expensive graphs | Compared current models with compact public variants on all available examples, randomized grids, and the local scorer where supported. | Frank `task243` was exact but cost `22650` vs current `10690`; Hoang `task286` was exact but `+77` cost; Poby/Ryosuke `task366` were about `+2272` cost; Poby `task090`, `task165`, and `task368` were also worse. Negative-pad tasks `task018`/`task240` remained locally unscorable. | Not submitted | Serialized byte count and node count are poor proxies for the competition metric; compact convolutional or cast-reduced graphs can consume more scored tensor memory. | Keep current versions. Revisit `task018`/`task240` only with a scorer that reproduces Kaggle's negative-padding environment. |
| 2026-07-13 | Broader unseen public-variant audit | Refreshed 100 current public kernels, downloaded Biohack, ChatGPTLoop, and task-level solver families, and compared unseen task variants directly against v3. | Biohack exposed 43 differences and the task-level release exposed 76 previously unseen variants; neither produced a scorer-positive validated replacement. ChatGPTLoop contained no unseen variants. | Not submitted | Public recombination is exhausted at the currently visible artifact set. Five Biohack and three task-level negative-padding alternatives remain scorer-unresolved, but none has positive evidence. | Stop broad public graft searches unless a genuinely newer or higher-scoring artifact appears. |
| 2026-07-13 | Dtype and deeper bitwise opportunity audit | Inventoried static intermediate memory by dtype over 378 shape-inference-compatible tasks and searched for associative/commutative CSE and generalized absorption in all bitwise circuits. | Theoretical ceiling: `+6.77` for universal `int64 -> int32`, `+45.95` for universal `float32 -> float16`; no additional bitwise identities were found. Most large `int64` tensors are mandated by ONNX index operators, so the practical ceiling is much lower. | Not submitted | Local dtype edits are not a medal-scale standalone strategy, and v3 exhausted the safe generic bitwise identities currently implemented. | Build task-specific low-memory algorithms rather than forcing schema-incompatible dtype changes. |
| 2026-07-13 | All-task semantic/ONNX family inventory | Combined train/test/ARC-gen transformation features with v3 graph cost and operator features for all 400 tasks. | Found 58 repeated coarse semantic signatures. Manual checks showed that high-level geometry tags can group unrelated tasks, while the inventory still surfaced `task243` as a high-cost same-shape paint target. | Not submitted | Useful as a routing index, not as evidence that two tasks share an implementation. Reports are under `references/analysis/task-families-v3/`. | Refine families with task-specific edit semantics before applying a shared compiler primitive. |
| 2026-07-13 | `task243` packed flood-fill carry resynthesis | Identified the rule as color 1 flooding through 4-connected cells whose color is 0 or 1. Replaced 136 six-node directional neighborhood chains with four-node exact carry closures while preserving the existing packed-row architecture. | Cost `10690 -> 9602`, projected `+0.107337`; exact on all 265 provided examples and 10,000 ordinary random grids. Across 10,000 dense stress mazes: 9,994 exact, 0 false-positive cells, 27 monotone false-negative cells. Archive passed canonical 400-file layout, zip CRC, 400/400 standard ONNX checker, and full check on changed `task243`. | Not submitted | Conservative semantic-dominance candidate: each rewrite only adds cells reachable within the same free row, so it cannot invalidate a case already solved by the scored model. Candidate SHA-256 `790dd11eade66211e3e44b49dbbcc981604d5116b68f944039ba8c91ab7ac033`. | Manual submission candidate at `submissions/task243-carry-v2-validated/submission.zip`; do not use the cheaper pass-5 cutoffs, which under-fill substantially more stress cases. |
| 2026-07-13 | Discussion-driven ARC-GEN validation of public v9 models | Downloaded the public `haonanzhengh/neurogolf-v9-models` archive, recovered Google's official ARC-GEN source, and tested compact replacements on fresh generator seeds. | Seventeen top variants passed 1,024/1,024 fresh examples each; `task188` and `task161` failed 2/128, while `task079` failed 1/128. The 15 scoreable accepted replacements projected `+25.813616`; `task045`/`task240` added unresolved upside. | 7299.68, submission ref `54660934` | Improved scored v3 by `+29.88`, validating fresh task-domain generation as a materially stronger hidden-test proxy than arbitrary random grids. | Apply the fresh-generator gate to the remaining v9 cost-positive variants before any broader graft. |
| 2026-07-13 | Second-wave fresh ARC-GEN v9 audit | Ranked all 400 v9 tasks against scored `7299.68`, screened 62 remaining cost-positive or base-unscorable variants on 128 fresh cases, then ran 896 additional independent cases for 55 survivors. | Ten variants failed the first screen or stress stage. Final set: 52 variants passing 1,024/1,024 fresh cases each; 46 measurable changes projected `+20.702662`, with six additional locally unscorable survivors. | 7319.89, submission ref `54661618` | Improved the scored base by `+20.21`, within `0.49` of the measurable projection, and became the new account best. | Use v2 as the scored base; do not reintroduce any rejected rare-failure variant. |
| 2026-07-13 | Cross-source scorer and equal-cost correctness audit | Deduplicated 21 canonical public archives into 699 task variants against scored `7319.89`. Seven were cost-positive; `task191` and `task233` failed the 128-case screen, while the other five passed 128 plus 896 independent cases. Twenty-three equal-cost task alternatives produced no correctness gain and `task193` regressed. | Final six-task package added the five survivors plus `task243`; combined local projection `+0.121980`. | 7320.02, submission ref `54661951` | Improved the scored base by `+0.13`, validating cross-source cost scoring only when paired with fresh-generation filtering. | Use the six-task package as the scored base. Do not reintroduce failed `task191`, `task233`, neutral `task193`, or unproven equal-cost substitutions. |

## Backlog

| Idea | Rationale | Expected impact | Cost | Priority |
| --- | --- | --- | --- | --- |
| More same-shape color logic | Many tasks preserve the nonzero mask or shape but require slightly richer local rules than a pure color map. | Medium: several quick extra task files. | Low-medium | High |
| Task001 Kronecker expansion | Task001 appears to apply an input-mask-driven expansion; likely a reusable pattern for several size-changing ARC tasks. | Medium if solved generically. | Medium | High |
| Public notebook/discussion mining | Competition has thousands of teams; accepted ONNX operator patterns may already be public. | High for speed. | Low-medium | High |
| Extend exact rewrite toolchain | Passes v2/v3 exhausted the first conservative rewrite set and found four more exact gains. The next credible axis is deeper associative bitwise-circuit simplification or scorer-aware operator substitution with exact differential validation. | Medium if targeted at the highest-cost circuits. | Medium-high | High |
| ONNX graph template library | Reusable builders for color maps, shifts, flips, crops, masks, and simple convolutions. | High over several days. | Medium | Medium |
| ARC-to-ONNX task resynthesis | `task243` produced the first proof-backed architecture candidate. Continue by inferring each task rule from train/test plus 262-ish `arc-gen` examples and compiling a low-memory program. Next high-cost targets are `task233`, `task366`, `task286`, `task054`, and `task133`. | Highest remaining upside; can improve correctness and architecture cost rather than only metadata. | High | Highest |

## Abandoned

| Approach | Why dropped | Evidence | Revisit if |
| --- | --- | --- | --- |
| Submit all task files as memorized constants | Private benchmark suite is explicitly designed to prevent overfitting, and constant test outputs would likely fail. | Official evaluation text requires correctness on original ARC-AGI plus private benchmark. | Only for local debugging, never as the main submission strategy. |
